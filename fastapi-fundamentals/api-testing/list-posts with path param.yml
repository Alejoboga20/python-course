info:
  name: list-posts with path param
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/posts/2"
  params:
    - name: include_content
      value: "true"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
