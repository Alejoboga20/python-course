info:
  name: list-posts with path param
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/posts/2?include_content=true"
  params:
    - name: title
      value: rails
      type: query
      disabled: true
    - name: include_content
      value: "true"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
