info:
  name: update post
  type: http
  seq: 5

http:
  method: PUT
  url: "{{base_url}}/posts/1"
  body:
    type: json
    data: |-
      {
          "title": "update title",
          "content": "update content"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
