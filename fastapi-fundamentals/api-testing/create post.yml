info:
  name: create post
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/posts"
  body:
    type: json
    data: |-
      {
          "title": "test title",
          "content": "test content"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
