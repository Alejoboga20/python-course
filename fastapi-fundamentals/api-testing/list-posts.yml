info:
  name: list-posts
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/posts"
  params:
    - name: title
      value: fastapi
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
